# System
alias a="sudo apt"
alias ainstall="sudo apt install"
alias aupdate="sudo apt update"
alias aupgrade="sudo apt upgrade"
alias aremove="sudo apt-get --purge autoremove"
alias alist="sudo apt list --upgradable"

alias zshrc="source ~/.config/zsh/.zshrc"
alias ls="exa -aF"
alias lsr="ranger"
alias v="nvim"
alias fd="fdfind"

alias pbcopy="xclip -selection clipboard"
alias ipinfo="curl https://ipinfo.io/"
alias remove="sudo apt-get --purge autoremove"
alias weather="curl https://wttr.in/"
alias tmux="tmux -u"

# Git
alias gi="git init"
alias gra="git remote add"
alias ga="git add"
alias gaa="git add --all"
alias gau="git add -u"
alias gs="git status"
alias gc="git commit -m"
alias gp="git push"
alias gpf="git push -f"
alias gpp="git push -u origin HEAD"
alias gl="git pull"
alias glu="git pull upstream"
alias gsh="git switch"
alias gsc="git switch -c"
alias glg="git log"
alias gbm="git branch -m"
alias gbd="git branch -D"
alias gcp="git cherry-pick"
alias gcl="git clone"
alias gsa="git stash"
alias gsp="git stash pop"
alias gcln="git clean -df"


alias gce="git commit -e -m"
alias gca="git commit -e --amend -n"
alias gcan="git commit --amend --no-edit -n"
alias gempty="git commit --allow-empty -m"

alias gd="git diff"
alias gds="git diff --staged"
alias gdp="gh pr diff"
alias gdf="git diff --name-only"
alias gdw="gh pr diff --web"

alias gr="git restore"
alias grs="git restore --staged"
alias grm="git rm -r --cached"
alias gbc="git branch --show-current | pbcopy"

alias gst="git stash push -m"
alias gslist="git stash list"

# Github
alias glist="gh pr list -A rocktimsaikia"
alias gmerge="gh pr merge"
alias gcheck="gh pr checks"
alias gcreate="gh pr create --assignee @me"
alias gview="gh release view"
alias gis="gh issue list"

# NPM
alias nI="npm install -g"
alias nU="npm uninstall -g"
alias nl="npm ls"
alias nL="npm ls -g"

# Docker
# alias dc="docker-compose"
# alias dc-remove-nones="docker rmi $(docker images -f 'dangling=true' -q)"

# Codingal
alias cg="cd ~/main"
alias cgw="cd ~/main/web"
alias cgp="cd ~/main/api"
alias cgpy="dc exec api ./manage.py shell_plus"
alias cgdb="docker exec -it cgdb mysql -u root -p"
alias cgpm="dc exec api manage.py migrate"
alias cgi="cd ~/main/web/public/images"
alias cgv="source ~/.pyenv/versions/codingal/bin/activate"

# Shortcutts

## Files
alias eA="nvim ~/.config/aliasrc"
alias eZ="nvim ~/.config/zsh/.zshrc"
alias eV='nvim ~/.config/nvim'
alias eK="nvim ~/.config/kitty/kitty.conf"

## Directories
alias ..="cd .."
alias D="cd ~/Downloads"
alias P="cd ~/Pictures"
alias cdot="cd ~/dotfiles"
alias al="cat ~/.config/aliasrc"
alias mkcd="source ~/scripts/mkd"

# Rofi
alias rofipower="rofi -show power-menu -modi power-menu:rofi-power-menu"

alias sp="sort-package-json"


# Experimental
alias grammerai="~/grammerai/dist/cli.mjs"

alias bat="bat -p"

alias aic="~/aicommits/dist/cli.mjs -at conventional"
alias dc="docker-compose"
alias conf="cd ~/.config"
alias cz="cz -n && gp"

# This is needed to make ssh work properly with kitty
# without issues like "terminal is not fully functional"
alias ssh="kitty +kitten ssh"

alias co="gh copilot"
