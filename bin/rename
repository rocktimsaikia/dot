#!/bin/bash


# Renaming files without needing to specify the whole path in the second argument.
# Works only when moving/renaming a file in the same directory.
FILE_PATH=$(dirname "$1")
mv "$1" "$FILE_PATH/$2"
